/* Robot Voice Commands */

// Gestisce i comandi ricevuti via voce


#include "comandiRobot.h"
#include "robot.h"
extern struct robot_c robot;
enum {

};
void processVioceCommand(int cmd, int context=0); {
	switch (context)
	{
	VCMD_0:

	default:
		break;
	}
	switch (switch_on)
	{
	VCMD_0:

	default:
		break;
	}

}
